# References - vercel: a platform for frontend frameworks and static sites.

# lab 16 steps:
# 1. git init, poetry init, poetry add requests
# 2. poetry export -o requirements.txt
# 3. poetry export -o requirements.txt --without-hashes
